package PresentationLayer.Controller.Listeners;

/**
 * Interface GetInformationUserListener that abstracts the methods that manages user's information to login.
 */
public interface GetInformationUserListener {
    /**
     * Method that notifies to the panel to throw the information from user.
     */
    void notifyPanelToGetInformation();
}
