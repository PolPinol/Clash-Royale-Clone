package PresentationLayer.Controller.Listeners;

/**
 * Interface GetDataTableClicked that abstracts the method to throw the information from the table.
 */
public interface GetDataTableClicked {
    /**
     * Method that notifies to the panel to throw the information from the table.
     */
    void notifyDataTableClicked();
}
