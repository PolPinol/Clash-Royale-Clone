package PresentationLayer.Controller.Listeners;

/**
 * Interface NextButtonListener that abstracts the method to listen the button.
 */
public interface NextButtonListener {
    /**
     * Method that notifies to the panel the button has been clicked.
     */
    void notifyButtonPressed();
}
